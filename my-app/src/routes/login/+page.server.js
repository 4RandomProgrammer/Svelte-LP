//Arquivo necessário para lidar requisições. N sei se do tipo get, mas do tipo post
/** @type {import('./$types').Actions} */

export const actions = {
    default: async (event) => {
        // TODO log the user in
    }
};